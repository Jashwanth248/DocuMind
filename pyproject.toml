[project]
name = "documind"
version = "0.1.0"
description = "Multimodal knowledge assistant for private document collections"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
